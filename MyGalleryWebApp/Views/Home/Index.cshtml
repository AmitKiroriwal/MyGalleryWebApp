
@model List<MyGalleryWebApp.Models.GalCategory>;
<title>Category Details</title>
<body>
   

<!-- bootstrap image gallery 1 -->
<div class="container-fluid">

  <div class="jumbotron">
    <h1 class="text-center">Categories</h1>
    <p class="text-center">Mountains make up about one-fifth of the world's landscape, and also provide homes to at least 10% of the world's people.</p>
  </div>
        <div class="row">
    @foreach (var item in Model)
    {
  
    <div class="col-sm-6 col-md-4 col-lg-3" id="divCategory" >
      <figure class="figImg">
                        <img src="~/Images/@item.CoverImage" class="grayscale img-fluid" id="imgfig">
                        <figcaption id="figc" > @Html.ActionLink(@item.Name, "Gallery","Home", new{id=item.Id}) </figcaption>
      </figure>
    </div>
    
    }
    </div>
</div>
 
  </body>

@section Scripts
    {
    <script type="text/javascript">

        $(document).ready(function () {


            $('#divCategory').click(function () {
                var CatId = $("#figc").val();
                alert(CatId);
                var Cname = $("#CatName");
                var baseUrl = window.location.origin;
                var settings = {
                    "url": baseUrl + "/Home/Gallery/" + CatId,
                    "method": "POST",
                    "timeout": 0

                };

                //$.ajax(settings).done(function (response) {

                //    if (response != null && !jQuery.isEmptyObject(response)) {
                //        Cname.empty();
                //        Cname.append($('<option/>', {
                //            value: "",
                //            text: "Select Category Name"
                //        }));
                //        //   var distJson = JSON.parse(response);

                //        $.each(response, function (i, res) {

                //            Cname.append($('<option/>', {
                //                //value: res.value,
                //                text: res.text

                //            }));
                //            // console.log(res);
                //        });

                //    };
                });
            });
        });


    </script>
    }